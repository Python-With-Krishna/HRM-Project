[project]
name = "hrm-project"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "krishna4241", email = "racherlakrishnaprasad4241@gmail.com" }
]
requires-python = ">=3.13"
dependencies = [
    "fastapi>=0.141.1",
]

[project.scripts]
hrm-project = "app.main:app"

[build-system]
requires = ["uv_build>=0.12.0,<0.13.0"]
build-backend = "uv_build"

[tool.uv.build-backend]
module-root = ""
module-name = "app"